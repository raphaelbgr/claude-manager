interface:
  display_name: "Claude Manager"
  short_description: "Repo-specific patterns and workflows for claude-manager"
  default_prompt: "Use the claude-manager repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true